# ビームサーチ関連のtips
## 評価関数
- 最終的なスコアに近似するように設計できるとベター
- 最終スコアの下界・上界を見積もるなど
- 評価関数は差分で高速に計算したい(O(width * depth * child_size)の評価関数が走る)
## 次ターンの候補追加
- 一手を大きくとる。(評価の変動までが遠い場合など)
- 次のターンの派生先を乱数でサンプリングする。(派生先の数が多すぎる場合に有効)
- 一つのノードからの派生先を貪欲に選んで厳選する。(多様性確保に寄与)
## ハッシュ
- 同一視したい状態を同一視できるようにハッシュを取る。
